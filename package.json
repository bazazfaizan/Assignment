{
  "name": "sdet-practical-assignment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test tests/ui/tests",
    "test:headed": "playwright test tests/ui/tests --headed",
    "test:api": "playwright test tests/api/tests",
    "test:all": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "zod": "^4.0.0"
  }
}
